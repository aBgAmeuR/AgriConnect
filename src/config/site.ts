import { SiteConfig } from '@/types';

export const siteConfig: SiteConfig = {
  title: 'AgriConnect',
  description: 'Plateforme de gestion et de distribution pour producteurs alimentaires locaux et leurs clients.',
};
